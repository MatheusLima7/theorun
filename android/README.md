# Android native project

Este diretório é gerado automaticamente pelo Expo. Para gerar os arquivos nativos completos, execute:

```bash
npx expo prebuild
```

Depois disso, você poderá editar os arquivos nativos em `android/`.
